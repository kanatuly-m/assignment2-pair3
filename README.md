# Assignment 2 — Pair 3

This project implements **Kadane's Algorithm** (Student B) with metrics and benchmarking.  
The partner implements **Boyer–Moore Majority Vote** (Student A).

## Structure
- `algorithms/` — core algorithms (`Kadane`, `Result`)
- `metrics/` — performance tracker
- `cli/` — benchmark runner
- `test/` — JUnit tests
- `docs/` — report and performance plots

## How to run
### Run tests
```bash
mvn test
